<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GetirFinalCase" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/bg_primary</item>
    </style>

    <style name="Theme.GetirFinalCase" parent="Base.Theme.GetirFinalCase" />

</resources>